import { makeStyles } from '@material-ui/core';

export default makeStyles({

    itemCountDiv: {
        fontFamily: 'Roboto',
        fontSize: 20,
        color: '#800000'
    },
    itemMsg: {
        color: 'grey',
        fontSize: 12,
        fontWeight: "Bold",
    }
})
